import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   container: {
      flex: 1,
      justifyContent: "center",
      alignItems: "center",
      padding: 20,
   },
   viewButton: {
      width: "100%",
      marginTop: 50,
   },
   textView: {
      fontSize: 20,
      fontWeight: "bold",
      color: "black",
   },
});
