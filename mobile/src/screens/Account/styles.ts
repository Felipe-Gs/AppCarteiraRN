import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   container: {
      flex: 1,
      alignItems: "center",
      marginTop: 50,
   },
   viewfoto: {
      justifyContent: "center",
      alignItems: "center",
   },
   viewText: {
      width: "100%",
      height: 250,
      padding: 20,
      marginTop: 40,
      justifyContent: "center",
   },
});
